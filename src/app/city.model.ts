export class City {
  name: string;
  temp: string;
  pressure: string;
  description: string;
  windSpeed: string;
  windDegree: string;
}

export interface cityWeather {
  name: string,
  temp: string;
  pressure: string;
  description: string;
  windSpeed: string;
  windDegree: string;
}